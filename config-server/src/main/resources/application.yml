spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          skip-ssl-validation: true
          uri: https://github.com/mahdiboughrous/rent-app-spring
          search-paths:
          - configs

server:
    port: 7777 